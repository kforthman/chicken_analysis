The four free parameters:

- J_default: governs the default hazard rate used on average. H_default = 1/(1+exp(-J_default)

- J_slope: m_h is the slope of the regression that describes the subject's adaptivity to hazard rate.

- noise in the DV: noise in the decision variable, L_n.

-lapse rate: lapses of attention